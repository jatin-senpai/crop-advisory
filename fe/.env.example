# Example environment variables for the frontend (Vite)
# Copy this to `.env` or set these in your hosting provider.

# Point this to your hosted backend (no trailing slash)
VITE_API_URL="https://crop-advisory-hlhq.onrender.com"

# Example: in Netlify or Vercel you can set VITE_API_URL in the project settings.
